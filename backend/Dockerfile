# T1 placeholder. Runtime image, Xray, geoip.dat, and geosite.dat arrive in the implementation task.
FROM python:3.12-slim
WORKDIR /app
COPY backend/app /app/backend/app
CMD ["python", "-m", "backend.app.main"]
