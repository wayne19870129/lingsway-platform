host: 203.0.113.10
domains:
  site: lingsway.com
  api: api.lingsway.com
  subscription: lingsway.com
providers:
  egress: mock
  accounting: mock
  gateway: mock
  forwarder: mock
  payment: mock
  notify: noop
  email: noop
  captcha: noop
  storage: local
